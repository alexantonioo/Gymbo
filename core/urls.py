# este archivo se definen las rutas de la aplicacion

# ejemplo :

""""
    from django.urls import path
    from . import views

    urlpatterns = [
        path('', views.home, name='home'),
        path('trainer/', views.trainer, name='trainer'),
        path('client/', views.client, name='client'),
    ]
"""

